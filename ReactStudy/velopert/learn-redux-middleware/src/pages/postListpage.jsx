import React from "react";
import PostListContainer from "../containers/postListContainer";

const PostListpage = (props) => {
  return <PostListContainer></PostListContainer>;
};

export default PostListpage;
